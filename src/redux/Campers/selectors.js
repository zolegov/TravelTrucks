export const selectTrucks = (state) => state.camper.trucks;
export const selectisLoading = (state) => state.camper.isLoading;
export const selecthasMore = (state) => state.camper.hasMore;
export const selectTruck = (state) => state.camper.truck;
export const selectFavorites = (state) => state.camper.favorites;
